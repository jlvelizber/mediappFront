# Copia a .env.production antes de `docker build` (imagen de esa instancia).
# No uses localhost: son las URLs publicas HTTPS que vera el navegador.

NEXT_PUBLIC_API_URL=https://api.tu-instancia.mediappec.com/api
NEXT_PUBLIC_PUBLIC_URL=https://tu-instancia.mediappec.com
NEXT_PUBLIC_TITLE=Mediapp
